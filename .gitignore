# Ignore Gradle project-specific cache directory
.gradle

# Ignore Gradle build output directory
build

# Ignore Kotlin plugin data
.kotlin

# Local secrets -- covers .env, .env.local, .env.production, etc.
# .env.example is the tracked template, so it stays committable.
.env
.env.*
!.env.example
